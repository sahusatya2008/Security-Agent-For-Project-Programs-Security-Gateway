# Dashboard Backend Note

The primary backend API is implemented in `acrs/api/` and exposed through FastAPI.
This folder is reserved for dashboard-specific adapters (auth, analytics, websocket fan-out) if needed.
